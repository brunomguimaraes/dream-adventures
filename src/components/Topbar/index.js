import React from 'react';

import Logo from '../Logo';

import * as Styled from './index.styles';

const Topbar = () => {
  return (
    <Styled.Topbar>
      <Logo />
    </Styled.Topbar>
  );
};

export default Topbar;
